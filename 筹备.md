![image-20260222164400235](figyres\image-20260222164400235.png)

![image-20260310160720328](figyres\image-20260310160720328.png)

![image-20260310161207855](figyres\image-20260310161207855.png)

路由器：负责不同广播域的广播隔离，实现不同广播域的单播通信

# osi参考模型：

应用层： 对应用程序提供接口

表示层：进行数据格式转换

会话层：在通信双方之间建立、管理、终止对话

传输层：建立、维护、取消一个端到端的数据传输过程

网络层：定义逻辑地址，实现数据从源地址到目的地转发

数据链路层：将数据封装成帧；在数据链路上实现数据的点到点，点到多的方式直接通信；差错检测

物理层：在媒介上传输比特流；提供机械的和电气的规约   

# tcp/ip模型：

应用层：Telnet  FTP TFTP SNMP HTTP SMTP DNS DHCP

传输层 TCP UDP

网络:ICMP IGMP  IP

数据链路:PPPOE ETHERNET PPP

物理层:   

## 传输层：   

### tcp 报文格式：

tcp：头部20 Byte 

源端口16bit 目的端口16bit

![image-20260310160809935](figyres\image-20260310160809935.png)

▫**Source Port：*****源端口****，标识哪个应用程序发送。长度为16比特。

▫**Destination Port*****目：的端口****，标识哪个应用程序接收。长度为16比特。

▫**Sequence Number**：**序号字段**。TCP链接中传输的数据流每个字节都编上一个序号。序号字段的值指的是本报文段所发送数据的第一个字节的序号。长度为32比特。

▫**Acknowledgment Number**：**确认序列号**，是期望收到对方下一个报文段数据的第1个字节的序号，即上次已成功接收到的数据段的最后一个字节数据的序号加1。只有Ack标识为1，此字段有效。长度为32比特。

▫**Header Length**：**头部长度**，指出TCP报文头部长度，以32比特（4字节）为计算单位。若无选项内容，则该字段为5，即头部为20字节。

▫**Reserved**：**保留**，必须填0。长度为6比特。

**▫Control bits**：**控制位**，包含**FIN**、**ACK**、**SYN**等标志位，代表不同状态下的TCP数据段。

**▫Window**：**窗口TCP的流量控制**，这个值表明当前接收端可接受的最大的数据总数（以字节为单位）。窗口最大为65535字节。长度为16比特。

**▫Checksum**：**校验字段**，是一个强制性的字段，由发端计算和存储，并由收端进行验证。在计算检验和时，要包括TCP头部和TCP数据，同时在TCP报文段的前面加上12字节的伪头部。长度为16比特。

▫**Urgent**:**紧急指针**，只有当URG标志置1时紧急指针才有效。TCP的紧急方式是发送端向另一端发送紧急数据的一种方式。紧急指针指出在本报文段中紧急数据共有多少个字节（紧急数据放在本报文段数据的最前面）。长度为16比特。

▫**Options**：**选项字段（可选**），长度为0-40字节。

•UDP报文头部：

▫**Source Port**:**源端口**，标识哪个应用程序发送。长度为16比特。

▫**Destination Port**:**目的端口**，标识哪个应用程序接收。长度为16比特。

▫**Length**:**该字段指定UDP报头和数据总共占用的长度**。可能的最小长度是8字节，因为UDP报头已经占用了8字节。由于这个字段的存在，UDP报文总长不可能超过65535字节（包括8字节的报头，和65527字节的数据）。

▫**Checksum**:**覆盖UDP头部和UDP数据的校验和，长度为16比特**。

端口号：0-65535

源端口号：1024-65535

目的端口号：0-1023  用来表示应用层的协议，通常每一个应用层协议在传输层分配一个端口号  (http:80 telnet:23 https:443  smtp:25 ftp:21(多通道协议，使用多个通道的协议))

目的端口：实现一个地址提供多种不同的应用服务

源端口：实现一个地址能够访问多种不同应用服务

### udp：8  Byte

### 三次握手：

![image-20260310160838189](figyres\image-20260310160838189.png)

![image-20260310160916960](figyres\image-20260310160916960.png)

1.sip	

dip tcp

sport:1024

dsport:80

seq num  =a

ack num=0

flag syn=1 ack=0



2.sip	

dip tcp

sport:80

dsport:1024

seq num  =b

ack num=a+1

flag syn=1 ack=1



3.sip	

dip tcp

sport:1024

dsport:80

seq num  =a+1

ack num=b+1

flag syn=1 ack=1

（ack=上一个seq+1）

### tcp建立后，传输数据

1. sip	

dip tcp

sport:1024

dsport:80

seq num  =a+1

ack num=b+1

flag syn=0 ack=1

http

data(20Byte)

 

2.sip	

dip tcp

sport:80

dsport:1024

seq num  =b+1

ack num=a+1+20

flag syn=0 ack=1

http

data(35Byte)



3.sip	

dip tcp

sport:1024

dsport:80

seq num  =a+1+20

ack num=b+1+35

flag syn=0 ack=1

http

data(40Byte)



4.sip	

dip tcp

sport:80

dsport:1024

seq num  =b+1+35

ack num=a+1+20+40

flag syn=0 ack=1

http

data(60Byte)a

### 可靠性机制：

1.三次握手之后，回应报文的ack num=接收报文的seq num+载荷大小

回应报文的seq num=接收报文的ack num

### 重传机制

发送tcp报文后，该报文在重传缓存中保存一份，当重传间隔超时的时候，没有收到正确的ack  num 的tcp报文，则重传该tcp报文

### 四次挥手:

![image-20260310160936668](figyres\image-20260310160936668.png)

## 应用层

为应用软件提供接口

pdu:数据协议单元

帧=数据帧（eth/ppp/FR/HDLC）+数据包：ip+**tcp/udp+pdu*****（数据段）****端口复用能力，提供可靠，高效传输

## 网络层

负责将数据报文从源主机发送到目的主机（负责数据从一台主机到另外一台主机的传递）

作用：提供逻辑地址

负责数据包的寻址和转发

如ipv4,ipv6 icmp igmp

tcp/ip模型中：所有的数据都是以帧的形式为单位，被设备进行处理；

百兆双绞线：
直通：568A-568A 568B-568B

568B（白橙橙白绿蓝白蓝绿白棕棕）

1,3 2,6互换

568A（白绿绿白橙蓝白蓝橙白棕棕）

交叉线：

568A-568B

千兆：
直通：568A-568A 568B-568B

交叉：1-3 2-6 4-7 5-8

hub 交换机 桥接设备

路由器 网卡 路由设备

交叉线：相同设备

直通线：不同设备

全反线：console



## CSMA/CD载波监听多路访问协议

hub组网中：csma/cd导致网卡工作在半双工状态

backoff随机退避算法：随机生成一个退避时间，在该时间超时后再发送数据，总线设备，冲突几率越大，backoff执行越来越频繁，发送机会越来越少，所有主机在冲突域中的数量越多，每个用户分配的宽带越少 



# 以太网交换基础

## 数据帧：



•在以太网![image-20260309220458488](figyres\image-20260309220458488.png)中，数据通信的基本单位是以太网帧 ( Frame )。以太帧的格式有两个标准：Ethernet_II格式和IEEE 802.3格式，因此协议规定以太网帧的数据格式如图所示。

•Ethernet Ⅱ以太帧：

▫DMAC：6字节，目的MAC地址，IPV4为6字节，该字段标识帧的接收者。

▫SMAC：6字节，源MAC地址，IPV4为6字节，该字段标识帧的发送者。

▫Type：2字节，协议类型。常见值：

▪0x0800：Internet Protocol Version 4 (IPv4) ；

▪0x0806：Address Resolution Protocol (ARP) 。

•IEEE 802.3 LLC以太帧：

▫逻辑链路控制LLC（Logical Link Control）由目的服务访问点DSAP（Destination Service Access Point）、源服务访问点SSAP（Source Service Access Point）和Control字段组成。

▪DSAP：1字节，目的服务访问点，若后面类型为IP值设为0x06。服务访问点的功能类似于Ethernet II帧中的Type字段或TCP/UDP传输协议中的端口号。

▪SSAP：1字节，源服务访问点，若后面类型为IP值设为0x06。 

▪Ctrl：1字节，该字段值通常设为0x03，表示无连接服务的IEEE 802.2无编号数据格式。

▫SNAP（Sub-network Access Protocol）由机构代码（Org Code）和类型（Type）字段组成。

▪Org Code三个字节都为0。

▪Type字段的含义与Ethernet_II帧中的Type字段相同。

•数据帧的总长度为64-1518字节，这样设计的原因是什么？（另外，以太网口的最大传输单元是1500字节，即MTU=1500B。）

▫以太网中，最小帧长为64字节，这是由最大传输距离和CSMA/CD机制共同决定的。

▪规定最小帧长是为了避免这种情况发生：A站点已经将一个数据包的最后一个Bit发送完毕，但这个报文的第一个Bit还没有传送到距离很远的B站点。B站点认为线路空闲继续发送数据，导致冲突。

▪高层协议必须保证Data域至少包含46字节，这样加上以太网帧头的14字节和帧尾的4字节校验码正好满足64字节的最小帧长，如果实际数据不足46个字节，则高层协议必须填充一些数据单元。

▫而出于对传输效率和传输可靠性的折中考虑，使得以太网帧的最大长度为1518字节，对应IP数据包就是1500字节。

▪较大的帧长度，数据的有效传输效率会更高；但是数据帧过长，传输时会占用共享链路过多的时间，对时延敏感应用造成极大的影响。

▪因此最终选择了一个折中的长度：1518字节的数据帧长，对应1500字节的IP数据包长度，这就是最大传输单元MTU的由来。

### 帧的宏观组成：

前导符+  8B

ETH type0800 + 14B（D.mac 6B+S.mac 6B+Type 2B）

ip+  20B

tcp  20B/udp  8B + 

pdu+ 变长

fcs+	4B

前导符 8B

链路层：负责将帧从链路的一个接口转发到链路上的另外一个接口

### MAC地址结构：

![image-20260309224759312](figyres\image-20260309224759312.png)